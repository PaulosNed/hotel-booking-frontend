export interface Room {
    room_id?: Number,
    id: Number,
    name: string,
    image: string,
    description: string,
    services: string,
    capacity: Number,
    bedNumber: Number,
    status: Boolean,
    price: Number,
}