export interface Room {
    room_id?: Number,
    start_date?: string,
    end_date?: string,
    id: Number,
    name: string,
    image: string,
    description: string,
    services: string,
    capacity: Number,
    bedNumber: Number,
    status: Boolean,
    price: Number,
}